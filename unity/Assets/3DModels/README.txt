Imported FBX meshes:

  - in import setting turn off 1cm file to 0.01m Unity
  - When imported into the scene, rotate arond X for -90 and scale X and Y with -1

Impored PLY PCs:

  - in import settings use Mesh as the Container Type

